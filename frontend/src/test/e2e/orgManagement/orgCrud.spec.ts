import {expect, test} from "@playwright/test";

test("Create New Organization", async ({page}) => {
    await page.goto("/");
    await test.step("Press New action element", async () => {});
    await test.step("Enter Org Information", async () => {});
    await test.step("Submit New Org form", async () => {});
});
